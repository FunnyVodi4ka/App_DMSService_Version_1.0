spring:
  config:
    activate:
      on-profile: test
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/changelog-master.yml
    parameters:
      CHANGELOG_DIR: db/changelog
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: false